import 'package:flutter/material.dart';

class EventsTiles extends StatelessWidget {
  final String title;
  const EventsTiles({super.key, required this.title});
  

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Hello Word"),
    );
  }
}